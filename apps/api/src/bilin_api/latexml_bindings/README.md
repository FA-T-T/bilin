# Bilin LaTeXML Bindings

This directory is added to LaTeXML's local search path before article sources.
Keep bindings minimal and display-preserving. Prefer the shared compatibility
registry and entry-file preamble shims for simple macros; add `.ltxml` bindings
only when package or environment behavior is too structured for safe string
rewrites.

