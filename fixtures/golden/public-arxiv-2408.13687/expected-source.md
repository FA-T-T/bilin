# Below-threshold memory

Quantum error correction combines physical qubits into a logical memory. The fixture keeps the structure of a real arXiv physics paper while using compact text suitable for deterministic regression tests.

$$
p_{\mathrm{L}}(d+2) \approx \frac{p_{\mathrm{L}}(d)}{\Lambda}
$$

# Real-time decoding

The paper reports real-time decoding alongside distance scaling. Bilin should preserve this paragraph as source Markdown and keep citation placeholders such as Ref. [1](#bib:acharya2024threshold).

**Figure 1.** A reduced surface-code memory schematic for parser regression.

**Table 1.** Representative structured values retained by the fixture.
